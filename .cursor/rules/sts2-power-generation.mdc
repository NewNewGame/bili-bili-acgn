---
description: STS2 能力生成规范（PowerModel）
globs: **/Powers/**/*.cs
alwaysApply: false
---

# 能力生成规则（Power）

- 继承 `PowerBaseModel`，使用 `public sealed class`；明确 `Type` 与 `StackType`。
- 能力效果通过 Hook 回调实现（如 `AfterCardPlayed`、`AfterDamageReceived`、`BeforeSideTurnStart`）。
- 回调内先做归属与条件判断（`target == base.Owner`、`cardPlay.Card.Owner == base.Owner.Player` 等），再执行效果。
- 数值来源优先使用 `base.Amount` 或 `base.DynamicVars`，不要硬编码动态变量。
- 临时能力请在恰当时机清理（如回合结束 `PowerCmd.Remove(this)`）。
- 若需要额外提示文本，使用 `ExtraHoverTips` 并复用 `HoverTipFactory`。

```csharp
public sealed class ExamplePower : PowerBaseModel
{
    public override PowerType Type => PowerType.Buff;
    public override PowerStackType StackType => PowerStackType.Counter;

    public override async Task AfterCardPlayed(PlayerChoiceContext context, CardPlay cardPlay)
    {
        if (cardPlay.Card.Owner == base.Owner.Player && cardPlay.Card.Type == CardType.Attack)
        {
            await CreatureCmd.GainBlock(base.Owner, base.Amount, ValueProp.Unpowered, null);
        }
    }
}
```
