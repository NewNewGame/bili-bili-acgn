---
description: BiliBiliACGN 模组卡牌（CardBaseModel）书写约定，仅本仓库
globs: BiliBiliACGNCode/Cards/**/*.cs
alwaysApply: false
---

# BiliBiliACGN 卡牌书写规则

新建/大段编写 `BiliBiliACGNCode/Cards/` 下卡牌类时遵循本文；**文件头**仍按 `.cursor/rules/bili-csharp-file-header.mdc`。

## 类型与结构

- 继承 `CardBaseModel`（`BiliBiliACGN.BiliBiliACGNCode.Cards`），`public sealed class`，**类名 = 文件名**。
- 构造函数：`base(energyCost, type, rarity, targetType, shouldShowInCardLibrary)`；战斗逻辑只放在 `OnPlay` / `OnUpgrade` / 少数专用回调（如 `OnTurnEndInHand`），**不要**在构造函数里打牌效。
- 用 `#region` 划分「关键词与悬停」「卡牌属性配置」等，与同目录现有牌一致。

## 卡池 `[Pool(typeof(...))]`

| 池 | 典型用途 |
|----|----------|
| `BottleCardPool` | 瓶子君 152 主卡池；新卡需在 `CardPool/BottleCardPool.cs` 的 `GenerateAllCards()` 里 `ModelDb.Card<YourCard>()` |
| `EventCardPool` | `CardRarity.Event` 的事件牌 |
| `CurseCardPool` | 诅咒牌（如 `CardType.Curse`、`energyCost = -1`、`Unplayable`） |
| `FanshikiCardPool` 等 | 其它 `CustomCardPoolModel`；同样需在对应 `GenerateAllCards` 注册 |

## 数值与词条

- 参考 `.cursor/rules/bili-csharp-file-header.mdc`

## 多人游戏专属

- 如果卡牌为多人游戏专属需要修改 `public override CardMultiplayerConstraint MultiplayerConstraint => CardMultiplayerConstraint.MultiplayerOnly;`

## 实现与命令

- 出牌： `protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)`；
- 伤害 `DamageCmd.Attack(...).FromCard(this)`+ (如果是单目标`.Targeting(...)`/全体`.TargetingAllOpponents(base.CombatState)`/随机`.TargetingRandomOpponents(base.CombatState)`)+(如果有攻击次数.WithHitCount(int))；
- 格挡 `CreatureCmd.GainBlock(choiceContext, cardPlay.Target(目标), base.DynamicVars.Block.BaseValue, base.DynamicVars.Block.Props, base.Owner(给予者), null);`
- 能力 `PowerCmd.Apply<T>()`；
- 抽牌 `CardPileCmd.Draw` 等（与同目录已实现牌一致）。
- 升级： `OnUpgrade()` 内 `DynamicVars[...].UpgradeValueBy(...)` 或 `base.EnergyCost.UpgradeBy(-1)`；诅咒若不可升级可 `MaxUpgradeLevel => 0`。
- X 费：energycost为-1，在OnPlay中通过`int num = ResolveEnergyXValue();`获得当前X的值，需要重写`protected override bool HasEnergyCostX => true;`。
- 诅咒：一般OnPlay方法为空，energycost为-1。
- 无法打出：卡牌的TargetType为None，CanonicalKeywords 添加`CardKeyword.Unplayable`。
- 卡牌发光显示：`protected override bool ShouldGlowGoldInternal`重写返回bool条件。
- 获取玩家牌：`PileType.Hand/Deck/Discard.GetPile(...)`
- 获取所有敌人：`CombatState.HittableEnemies`

## 本地化与资源

- 中文文案：`BiliBiliACGN/localization/zhs/cards.json`，键格式 **`BILIBILIACGN-{CLASS_NAME_UPPER_SNAKE}.title` / `.description`**，与 `DynamicVar` 占位符一致（如 `{Damage:diff()}`）。
- 卡图：`CardBaseModel` 按 `Id` 解析为 `card_portraits/`、`card_portraits_full/` 下小写 png；缺图会落到 `none.png`。

## 禁止/注意

- 不要与 `MegaCrit.Sts2.Core.Models.Powers` 里同名类型裸名冲突（如 `RagePower`）；用别名或只 import 一侧。
- 仅改卡牌时**不要**顺带改无关卡池/全局补丁；新卡**必须**接入对应 `*CardPool.GenerateAllCards`（若属于该池）。
