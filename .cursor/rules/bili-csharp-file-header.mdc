---
description: BiliBiliACGN 模组 C# 文件头申明（创建/新建 .cs 时必填）
globs: BiliBiliACGNCode/**/*.cs
alwaysApply: false
---

# C# 文件头申明

在 **`BiliBiliACGNCode/`** 下**新建**或**从零创建** `.cs` 文件时，文件**最顶部**（在 `using`、命名空间之前）必须包含下列格式的申明块；`描述` 用一句话说明该文件职责。

## 格式范例

```csharp
//****************** 代码文件申明 ***********************
//* 文件：PowerBaseModel
//* 作者：wheat
//* 创建时间：2026/03/26 10:51:27 星期四
//* 描述：Power基类模型，提供Power图片路径等通用能力
//*******************************************************
```

## 字段说明

| 行 | 内容 |
|----|------|
| 文件 | 与文件名一致，**不含** `.cs` 扩展名（如 `TestCard`） |
| 作者 | `wheat`（与项目惯例一致） |
| 创建时间 | `yyyy/MM/dd HH:mm:ss` + 星期中文（如 `星期四`） |
| 描述 | 简短中文，说明类/文件用途 |

## 注意

- 若仅在已有文件上做小改动，**不要**为符合本规则而批量改历史文件头。
- `tools/` 等独立工具链下的 C# 不在此 glob 范围内；若用户明确要求，再单独处理。


## 注释一律用中文
