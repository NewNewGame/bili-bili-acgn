---
description: STS2 卡牌生成规范（CardModel）
globs: **/Cards/**/*.cs
alwaysApply: false
---

# 卡牌生成规则（Card）

- 继承 `CardBaseModel`，类名与文件名保持一致，使用 `public sealed class`。
- 在构造函数中只定义基础元数据：`base(费用, CardType, CardRarity, TargetType)`。
- 变量统一放在 `CanonicalVars`，优先使用现有变量类型（如 `DamageVar`、`BlockVar`、`CardsVar`、`RepeatVar`）。
- 牌效统一放在 `OnPlay(PlayerChoiceContext, CardPlay)`；不要在构造函数里做战斗逻辑。
- 升级统一放在 `OnUpgrade()`，仅修改数值（`UpgradeValueBy`）或必要标签，不做复杂流程。
- 若牌面含“造成伤害后抽牌”等顺序效果，按 `OnPlay` 中调用顺序显式编排，避免隐式触发假设。

```csharp
public sealed class ExampleCard : CardBaseModel
{
    /// <summary>
    /// 牌面动态变量配置
    /// </summary>
    protected override IEnumerable<DynamicVar> CanonicalVars => 
    [
        new DamageVar(6m, ValueProp.Move),
        new CardsVar(1)
    ];

    public ExampleCard() : base(1, CardType.Attack, CardRarity.Common, TargetType.AnyEnemy) { }

    /// <summary>
    /// 出牌效果
    /// </summary>
    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        await DamageCmd.Attack(base.DynamicVars.Damage.BaseValue).FromCard(this).Targeting(cardPlay.Target).Execute(choiceContext);
        await CardPileCmd.Draw(choiceContext, base.DynamicVars.Cards.BaseValue, base.Owner);
    }

    /// <summary>
    /// 升级效果
    /// </summary>
    protected override void OnUpgrade() => base.DynamicVars.Damage.UpgradeValueBy(3m);
}
```
