<Window x:Class="CardEditorGui.KeywordEditorSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keywords 编辑器设置"
        Height="240" Width="620"
        MinHeight="220" MinWidth="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="card_keywords.json 保存路径" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBlock TextWrapping="Wrap" Foreground="Gray" FontSize="11" Margin="0,0,0,4"
                       Text="用于保存 title/description；不存在则创建，已存在则更新键值。" />
            <DockPanel LastChildFill="True" Margin="0,0,0,12">
                <Button DockPanel.Dock="Right" Content="浏览…" Width="80" Margin="8,0,0,0" Click="BtnBrowseKeywordsJson_Click" />
                <TextBox x:Name="TxtKeywordsJsonPath" />
            </DockPanel>

            <TextBlock Text="Key 前缀（命名空间）" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBox x:Name="TxtPrefix" ToolTip="例如 BILIBILIACGN（保存时自动转大写）" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="TxtSettingsPath" VerticalAlignment="Center" Margin="0,0,16,0"
                       Foreground="Gray" FontSize="11" TextTrimming="CharacterEllipsis" />
            <Button Content="确定" Width="88" Margin="0,0,8,0" IsDefault="True" Click="BtnOk_Click" />
            <Button Content="取消" Width="88" IsCancel="True" Click="BtnCancel_Click" />
        </StackPanel>
    </Grid>
</Window>

